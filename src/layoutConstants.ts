// Reused across every section so spacing stays consistent site-wide — change the
// rhythm once here instead of hunting down every py/scrollMarginTop pair.
export const SECTION_Y = { xs: 11, md: 18 };
export const SECTION_SCROLL_OFFSET = { xs: 84, md: 104 }; // clears the fixed header on #anchor jumps
